[pytest]
testpaths = tests
# No global timeout: the release gate's empirical audit performs a real silent install, a UI
# launch and an uninstall, which legitimately takes minutes. A short default timeout would
# fail the most valuable test in the suite for taking the time it needs.
